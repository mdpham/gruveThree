UserPlaylists = React.createClass({
	//Add Meteor methods to update a Playlists collection similar to Favorites for Meteor access to it
	render() {
		console.log("playlists", this.props.favoriter);
		return (<div></div>);
	}
});